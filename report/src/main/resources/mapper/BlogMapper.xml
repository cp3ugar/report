<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="indi.xk.report.mapper.BlogMapper">
    <insert id="addBlog">
        insert into blog
        (
            user_id,
            content
        )
        values
        (
            #{userId},
            #{content}
        )
    </insert>

    <update id="updateBlog">
        update blog
        set content = #{content}
        where id = #{id}
    </update>

    <delete id="deleteBlog">
        delete from blog
        where id = #{id}
    </delete>

    <select id="detailBlog" resultType="indi.xk.report.pojo.Blog">
        select
        b.id,
        b.content,
        b.user_id,
        u.`name` userName
        from blog b
        left join `user` u
        on b.user_id = u.userId
        where id = #{id}
    </select>

    <!-- 列表数量 -->
    <select id="count" resultType="Integer">
        select count(*) from blog
    </select>

    <!-- 列表 -->
    <select id="listBlog" resultType="indi.xk.report.pojo.Blog">
        select
        b.id,
        b.user_id,
        b.content,
        u.`name` userName
        from blog b
        left join `user` u
        on b.user_id = u.userId
        <if test="pageView != null">
            LIMIT #{pageView.startPage},#{pageView.pageSize}
        </if>
    </select>
</mapper>